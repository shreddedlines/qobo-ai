# Dependencies and build output
node_modules/
dist/
coverage/

# Secrets — only the example file is committed
.env
.env.*
!.env.example
!.env.test.example
!.env.prod.example
!.env.smoke.example

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# Playwright
playwright-report/
test-results/

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Knowledge-base build previews (derived, not committed)
kb/.build/

# Downloaded Supabase CA certificates (public, but machine-specific)
api/certs/

# Generated eval reports (full model answers); publish deliberately, never by default
eval/results/
